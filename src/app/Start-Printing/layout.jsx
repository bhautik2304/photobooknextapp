export const metadata = {
  title: "Photokrafft - Order Product",
  description: "Begin your album printing journey here.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
