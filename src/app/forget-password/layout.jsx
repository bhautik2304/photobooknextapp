export const metadata = {
  title: "Photokrafft - Forget-password",
  description: "Begin your album printing journey here.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
